# URL-Shortener
For running BE(api) inside the folder api/
run these commands:
1.npm install
2.npm run dev 

For running FE(client) inside the folder client/
run these commands:
1.npm install
2.npm start
 
